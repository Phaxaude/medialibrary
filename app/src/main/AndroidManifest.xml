<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- For Android 12 (API 32) and lower -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- For Android 13 (API 33) and higher -->
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- APPLICATION BLOCK START -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MediaLibrary">


        <!-- The Main Home Screen -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- The Image Screens -->
        <activity android:name=".ImageFoldersActivity" android:exported="false" />
        <activity android:name=".FolderViewActivity" android:exported="false" />

        <!-- The Video Screens -->
        <activity android:name=".VideosActivity" android:exported="false" />

        <!-- The Video Player Screen -->
        <activity android:name=".VideoPlayerActivity" android:exported="false" />

    </application>
        <!-- APPLICATION BLOCK END -->

</manifest>